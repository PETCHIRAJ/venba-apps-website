---
import Layout from '../components/Layout.astro';
import Header from '../components/Header.astro';
import AppCard from '../components/AppCard.astro';
import Footer from '../components/Footer.astro';
import appsData from '../data/apps.json';
---

<Layout>
    <Header />

    <section class="hero">
        <p>I build mobile applications that solve real problems with simple, intuitive designs.</p>
    </section>

    <section class="apps">
        <h2>My Apps</h2>
        <div class="app-grid">
            {appsData.apps.map((app) => (
                <AppCard
                    icon={app.icon}
                    name={app.name}
                    category={app.category}
                    description={app.description}
                    features={app.features}
                    storeLink={app.storeLink}
                    status={app.status as 'available' | 'coming-soon'}
                    isPlaceholder={app.isPlaceholder}
                />
            ))}
        </div>
    </section>

    <section class="about">
        <h2>About</h2>
        <p>I'm a solo developer focused on creating mobile applications that make daily tasks easier. I believe in simple design, user privacy, and practical functionality.</p>
        <p>My apps feature local language support and are designed to work seamlessly across different regions and cultures.</p>
    </section>

    <section class="contact">
        <h2>Get in Touch</h2>
        <p>Have questions or feedback? I'd love to hear from you!</p>
        <p>Email: <a href="mailto:contact.venbaapps@gmail.com">contact.venbaapps@gmail.com</a></p>
        <p>Play Store: <a href="https://play.google.com/store/apps/dev?id=8464608221563186460" target="_blank">View all apps on Google Play</a></p>
        <p>Blog: <a href="https://blog.venba.dev" target="_blank">Read my thoughts on tech and finance</a></p>
    </section>

    <Footer />
</Layout>
